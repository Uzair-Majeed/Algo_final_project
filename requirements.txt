# Disaster Relief Optimization System
# Python Requirements

# Core Dependencies
# No external libraries required for core functionality
# The system uses only Python standard library for main algorithms

# Optional Dependencies for Visualization (Question 6 - Bonus)
matplotlib>=3.5.0
networkx>=2.6.0

# Optional Dependencies for Enhanced Features
numpy>=1.21.0

# Development Dependencies (Optional)
# pytest>=7.0.0        # For unit testing
# black>=22.0.0        # For code formatting
# pylint>=2.12.0       # For code linting

# Notes:
# - Core system (Q1-Q5) works with Python 3.8+ standard library only
# - Visualization (Q6 bonus) requires matplotlib
# - NetworkX is optional (not used in current implementation)
# - NumPy is optional but may improve performance for large datasets

# Installation:
# pip install -r requirements.txt

# Minimal Installation (core only):
# No installation needed - uses Python standard library

# Full Installation (including visualization):
# pip install matplotlib networkx numpy

